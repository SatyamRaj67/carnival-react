# Git Branching Rules

git checkout -b feat/feature you added
git add .
git commit -m "feat: add feature you added"
git push

go to github and create a pull request to main branch

git checkout main
git pull
